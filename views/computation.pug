extends layout

block content
  h1= title
  p Welcome to #{title}
  p Math.abs() applied to #{x} is #{a};
  p Math.acos() applied to #{x} is #{b};
  p Math.sin() applied to #{x} is #{c};
  p Math.sinh() applied to #{x} is #{d};